import { init as array } from './MyArray.js';
import { init as object } from './HashTable.js';
import { init as linked } from './LinkedList.js';
import { init as stack } from './Stack.js';

export default {
  array,
  object,
  linked,
  stack,
}